export interface Certificate {
  name: string;
  code: string;
}

export interface SisterCompany {
  name: string;
  img: string;
  description?: string;
}

export interface FounderData {
  name: string;
  role: string;
  image: string;
}
